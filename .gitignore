# Ignore everything
*

# Except this file
!/.gitignore

# Except the site folder
!/site
!/site/**

# The Leaving (second project) working material — intentionally kept out of git
# (already covered by the catch-all above; listed explicitly to record intent)
/the-leaving/
/the-leaving/**

# The second work (untitled mystery, 1978-1993) — same intent as the-leaving
/the-second-work/
/the-second-work/**

# System files
**/.DS_Store

# Keep .env out of git (contains secrets)
/site/artifact-catalog/.env
